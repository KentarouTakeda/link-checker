import { parse, parseLinksFromUrl } from './parseLinks';

export async function scrape(url: string, log: Function|undefined = console.info, limit: number = Infinity, i: number = 0): Promise<parse[]> {
	return [];
}
